-是否支持下标索引：
支持：列表、元组、字符串 - 序列类型
不支持：集合、字典 - 非序列类型
-是否支持重复元素：
支持：列表、元组、字符串 - 序列类型
不支持：集合、字典 - 非序列类型
-是否可以修改：
支持：列表、集合、字典
不支持：元组、字符串
使用场景：
列表：可修改、可重复的一批数据记录场景
元组：不可修改、可重复的一批数据记录场景
字符串：一串字符串的记录场景
集合：不可重复的数据记录场景
字典：以Key检索Value的数据记录场景

都支持循环遍历
都支持for循环。
因集合、字典没有下标索引所以不支持while循环

通用操作：
len(容器)：统计元素个数
max(容器)：统计最大元素
min(容器)：统计最小元素

容器通用转化功能：
list()
str()
tuple()
set()
不能转字典（字典为键值对，但字典可以转别的类型）
只有字典转字符串Value的值还在

容器的通用排序功能：
sorted(容器, reverse = True)# 如果反转排序用逗号后的
结果放入列表中

对于嵌套列表，进行排序的依据是内层列表的第二个元素，所以sorted方法就无法使用了
所以用sort方法
